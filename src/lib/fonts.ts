import { Inter, Oswald, Roboto_Slab } from "next/font/google";

export const inter = Inter({
  subsets: ["latin", "latin-ext"],
  display: "swap",
});

export const oswald = Oswald({
  subsets: ["latin", "latin-ext"],
  display: "swap",
});

export const brandFont = Roboto_Slab({
  subsets: ["latin", "latin-ext"],
  display: "swap",
  weight: ["700", "800", "900"],
});